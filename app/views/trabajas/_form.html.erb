<%= form_with(model: trabaja, local: true) do |form| %>
  <% if trabaja.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trabaja.errors.count, "error") %> prohibited this trabaja from being saved:</h2>

      <ul>
      <% trabaja.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
